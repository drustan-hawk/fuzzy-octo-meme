# Fuzzy Octo Meme

A simple PySide6 thumbnail viewer.

## Usage

Run `python thumbnail_viewer.py` and choose a folder containing images. The
application will display thumbnails for the image files it finds. The main
window hosts a reusable `ThumbnailViewer` widget that can be embedded in other
PySide6 applications.
